[package]
name = "rpc-api"
version = "0.1.0"
license = "MIT"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0.12"
indexmap = "2.0"
types = { path = "../types" }
