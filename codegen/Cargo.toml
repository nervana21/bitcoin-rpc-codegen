[package]
name = "codegen"
version = "0.1.0"
edition = "2021"

[dependencies]
rpc_api = { path = "../rpc_api" }
transport = { path = "../transport" }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.5.0"
